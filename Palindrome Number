def isPalindrome(self, x: int) -> bool:
        num=x
        revnum=0
        if x<0:
            return False
        while x>0:
            ld=x%10
            revnum=(revnum*10)+ld
            x=x//10
        return revnum==num   
