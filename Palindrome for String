def isPalindrome(self, s: str) -> bool:
        
        s= ''.join(char.lower() for char in s if char.isalnum())
        revstr=(s[::-1])
        if revstr==s:
            return True
        else:
            return False
